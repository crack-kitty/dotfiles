repo: /absolute/path/to/target/repo
verify: exact shell command that exits nonzero on failure
auto_merge: false

# Title of the change

## Goal

One paragraph. What exists after this build that does not exist now.

## Acceptance

- Observable behavior 1, phrased so the verify command can check it
- Observable behavior 2

## Constraints

- Anything the builder must not touch or must match (patterns, deps, style)
